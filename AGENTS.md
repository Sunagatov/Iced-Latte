# Agent Instructions

- Follow the existing Java and Spring Boot style in this repository.
- Use Palantir Java Format through Maven for Java formatting.
- Do not reformat unrelated files.
- Before finalizing code changes, run `mvn spotless:apply` for touched files and then the relevant Maven build command.
- The normal local verification path is `mvn clean package`.
