<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="application_name" source="spring.application.name"/>
    <springProperty scope="context" name="application_version" source="spring.application.version" defaultValue="unknown"/>
    <springProperty scope="context" name="environment" source="spring.profiles.active" defaultValue="local"/>

    <!-- ========================================== -->
    <!-- CONSOLE APPENDER (Human-Readable)         -->
    <!-- ========================================== -->
    <appender name="ConsoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss,SSS} [%thread] %-5level %-40logger{40} userId=%X{userId} correlationId=%X{correlationId} requestId=%X{requestId} sessionId=%X{sessionId} clientTraceId=%X{clientTraceId} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ========================================== -->
    <!-- FILE APPENDER (JSON Format)               -->
    <!-- ========================================== -->
    <appender name="FileAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/iced-latte.log</file>

        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <version/>
                <logLevel/>
                <message/>
                <loggerName/>
                <threadName/>
                <context/>
                <pattern>
                    <omitEmptyFields>true</omitEmptyFields>
                    <pattern>
                        {
                        "application": "${application_name}",
                        "version": "${application_version}",
                        "environment": "${environment}",
                        "trace": {
                        "userId": "%X{userId}",
                        "correlationId": "%X{correlationId}",
                        "sessionId": "%X{sessionId}",
                        "requestId": "%X{requestId}"
                        }
                        }
                    </pattern>
                </pattern>
                <mdc>
                    <excludeMdcKeyName>traceId</excludeMdcKeyName>
                    <excludeMdcKeyName>spanId</excludeMdcKeyName>
                    <excludeMdcKeyName>parentId</excludeMdcKeyName>
                </mdc>
                <stackTrace/>
            </providers>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/iced-latte-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- ========================================== -->
    <!-- GRAFANA LOKI APPENDER                     -->
    <!-- ========================================== -->
    <springProperty scope="context" name="loki_enabled" source="logging.loki.enabled" defaultValue="false"/>
    <springProperty scope="context" name="loki_url" source="logging.loki.url" defaultValue="http://localhost:3100/loki/api/v1/push"/>
    <springProperty scope="context" name="application_label" source="spring.application.label" defaultValue="iced-latte-application"/>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>loki_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="LokiAppender" class="com.github.loki4j.logback.Loki4jAppender">
                <http>
                    <url>${loki_url}</url>
                </http>
                <labels>application=${application_label}
                    environment=${environment}
                    level=%level
                </labels>
                <message class="ch.qos.logback.classic.PatternLayout">
                    <pattern>level=%level application=${application_name} version=${application_version}
                        environment=${environment} logger=%logger thread=%thread traceId=%X{traceId} spanId=%X{spanId}
                        userId=%X{userId} correlationId=%X{correlationId} requestId=%X{requestId} message=%msg%n%ex
                    </pattern>
                </message>
            </appender>
        </then>
    </if>

    <!-- ========================================== -->
    <!-- ELASTICSEARCH/LOGSTASH APPENDER           -->
    <!-- ========================================== -->
    <springProperty scope="context" name="logstash_enabled" source="logging.logstash.enabled" defaultValue="false"/>
    <springProperty scope="context" name="logstash_host" source="logging.logstash.host" defaultValue="localhost"/>
    <springProperty scope="context" name="logstash_port" source="logging.logstash.port" defaultValue="5000"/>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>logstash_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="LogstashAppender" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
                <destination>${logstash_host}:${logstash_port}</destination>
                <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                    <providers>
                        <timestamp>
                            <timeZone>UTC</timeZone>
                        </timestamp>
                        <version/>
                        <logLevel/>
                        <message/>
                        <loggerName/>
                        <threadName/>
                        <pattern>
                            <omitEmptyFields>true</omitEmptyFields>
                            <pattern>
                                {
                                "application": "${application_name}",
                                "version": "${application_version}",
                                "environment": "${environment}",
                                "traceId": "%X{traceId}",
                                "spanId": "%X{spanId}",
                                "userId": "%X{userId}",
                                "correlationId": "%X{correlationId}",
                                "sessionId": "%X{sessionId}",
                                "requestId": "%X{requestId}",
                                "clientTraceId": "%X{clientTraceId}"
                                }
                            </pattern>
                        </pattern>
                        <mdc/>
                        <stackTrace/>
                    </providers>
                </encoder>
                <keepAliveDuration>5 minutes</keepAliveDuration>
            </appender>
        </then>
    </if>

    <!-- ========================================== -->
    <!-- DATADOG APPENDER                          -->
    <!-- ========================================== -->
    <springProperty scope="context" name="datadog_enabled" source="logging.datadog.enabled" defaultValue="false"/>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>datadog_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="DatadogAppender" class="ch.qos.logback.core.ConsoleAppender">
                <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                    <providers>
                        <timestamp>
                            <timeZone>UTC</timeZone>
                            <fieldName>timestamp</fieldName>
                        </timestamp>
                        <logLevel>
                            <fieldName>status</fieldName>
                        </logLevel>
                        <message>
                            <fieldName>message</fieldName>
                        </message>
                        <loggerName>
                            <fieldName>logger.name</fieldName>
                        </loggerName>
                        <threadName>
                            <fieldName>logger.thread_name</fieldName>
                        </threadName>
                        <pattern>
                            <omitEmptyFields>true</omitEmptyFields>
                            <pattern>
                                {
                                "ddsource": "java",
                                "service": "${application_name}",
                                "version": "${application_version}",
                                "env": "${environment}",
                                "dd.trace_id": "%X{traceId}",
                                "dd.span_id": "%X{spanId}",
                                "usr.id": "%X{userId}",
                                "correlation_id": "%X{correlationId}",
                                "session_id": "%X{sessionId}",
                                "request_id": "%X{requestId}",
                                "client_trace_id": "%X{clientTraceId}"
                                }
                            </pattern>
                        </pattern>
                        <mdc/>
                        <stackTrace/>
                    </providers>
                </encoder>
            </appender>
        </then>
    </if>

    <!-- ========================================== -->
    <!-- ASYNC WRAPPERS                            -->
    <!-- ========================================== -->
    <appender name="AsyncConsole" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="ConsoleAppender"/>
    </appender>

    <appender name="AsyncFile" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="FileAppender"/>
    </appender>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>loki_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="AsyncLoki" class="ch.qos.logback.classic.AsyncAppender">
                <queueSize>1000</queueSize>
                <discardingThreshold>0</discardingThreshold>
                <neverBlock>true</neverBlock>
                <appender-ref ref="LokiAppender"/>
            </appender>
        </then>
    </if>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>logstash_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="AsyncLogstash" class="ch.qos.logback.classic.AsyncAppender">
                <queueSize>1000</queueSize>
                <discardingThreshold>0</discardingThreshold>
                <neverBlock>true</neverBlock>
                <appender-ref ref="LogstashAppender"/>
            </appender>
        </then>
    </if>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>datadog_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <appender name="AsyncDatadog" class="ch.qos.logback.classic.AsyncAppender">
                <queueSize>1000</queueSize>
                <discardingThreshold>0</discardingThreshold>
                <neverBlock>true</neverBlock>
                <appender-ref ref="DatadogAppender"/>
            </appender>
        </then>
    </if>

    <!-- ========================================== -->
    <!-- LOGGER SUPPRESSIONS                       -->
    <!-- ========================================== -->
    <springProfile name="prod">
        <logger name="io.opentelemetry.exporter.internal.http.HttpExporter" level="WARN"/>
    </springProfile>
    <springProfile name="!prod">
        <logger name="io.opentelemetry.exporter.internal.http.HttpExporter" level="OFF"/>
    </springProfile>

    <logger name="org.springframework.data.repository.config.RepositoryConfigurationExtensionSupport" level="WARN"/>
    <logger name="org.springframework.data.repository.config.RepositoryConfigurationDelegate" level="WARN"/>

    <logger name="org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer" level="ERROR"/>
    <logger name="org.springframework.security.config.annotation.authentication.configuration.InitializeAuthenticationProviderBeanManagerConfigurer" level="ERROR"/>

    <logger name="com.github.loki4j" level="WARN"/>

    <!-- ========================================== -->
    <!-- ROOT LOGGER                               -->
    <!-- ========================================== -->
    <root level="INFO">
        <appender-ref ref="AsyncConsole"/>
        <appender-ref ref="AsyncFile"/>
    </root>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>loki_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <root level="INFO">
                <appender-ref ref="AsyncLoki"/>
            </root>
        </then>
    </if>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>logstash_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <root level="INFO">
                <appender-ref ref="AsyncLogstash"/>
            </root>
        </then>
    </if>

    <if>
        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>datadog_enabled</key>
            <value>true</value>
        </condition>
        <then>
            <root level="INFO">
                <appender-ref ref="AsyncDatadog"/>
            </root>
        </then>
    </if>

</configuration>